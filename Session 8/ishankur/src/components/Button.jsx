import React from 'react'
import '../styles/Button.css';

const Button = (props) => {
  return (
    <div>
        <button className='btn'>{props.rohit}</button>
    </div>
  )
}

export default Button